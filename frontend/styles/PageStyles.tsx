import { StyleSheet } from "react-native";

export const PageStyles = StyleSheet.create({
  page: {
    marginVertical: 16,
    marginHorizontal: 12,
    display: "flex",
    flexDirection: "column",
  },
});

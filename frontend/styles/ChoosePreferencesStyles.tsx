import { StyleSheet } from "react-native";

export const ChoosePreferencesStyles = StyleSheet.create({
  categoryContainer: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 16,
  },
});
